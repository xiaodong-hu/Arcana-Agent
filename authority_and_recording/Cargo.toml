[package]
name = "authority_and_recording"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.10"
glob-match = "0.2"
toml = "0.8"
